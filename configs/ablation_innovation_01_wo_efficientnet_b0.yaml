experiment:
  name: ablation_innovation_01_wo_efficientnet_b0
  mode: enhanced
  seed: 2026

runtime:
  allow_cpu_fallback: false

model:
  ensemble_mode: resnet

training:
  epochs: 150
  compile: true
  compile_mode: reduce-overhead
  early_stopping_patience: 16
  early_stopping_min_delta: 0.01

