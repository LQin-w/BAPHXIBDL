experiment:
  name: ablation_innovation_03_wo_cbam
  mode: enhanced
  seed: 2026

runtime:
  allow_cpu_fallback: false

model:
  cbam:
    enabled: false
    global_branch: false
    local_branch: false

training:
  epochs: 150
  compile: true
  compile_mode: reduce-overhead
  early_stopping_patience: 16
  early_stopping_min_delta: 0.01

