experiment:
  name: ablation_innovation_04_wo_local_branch
  mode: enhanced
  seed: 2026

runtime:
  allow_cpu_fallback: false

model:
  branch_mode: global_only
  cbam:
    local_branch: false

training:
  epochs: 150
  compile: true
  compile_mode: reduce-overhead
  early_stopping_patience: 16
  early_stopping_min_delta: 0.01

